import { Box } from "@mui/material";
import AddGroundForm from "../components/form-block/AddGroundForm";

function AddGroundPage() {
   return (
      <Box
         sx={{padding:' 50px 0 100px'}}
      >
         <AddGroundForm />
      </Box>
   );
}

export default AddGroundPage;
